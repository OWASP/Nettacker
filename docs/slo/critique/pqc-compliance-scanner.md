# Critique — pqc-compliance-scanner runbook

Four-persona adversarial review of [docs/RUNBOOK-pqc-compliance-scanner.md](../../RUNBOOK-pqc-compliance-scanner.md). Run 2026-05-06.

## Findings table

| id | persona | category | runbook section | finding | concrete scenario | recommendation |
|---|---|---|---|---|---|---|
| F-CEO-1 | CEO | ask | M1/M2 `TLS_PQC_NAMED_GROUPS` table; M3 verdict mapping (CNSA 2.0 row) | v1 table excludes `mlkem1024` (codepoint 0x0202), but the `compliance_notes` string in M3 explicitly maps `pqc_ready` to CNSA 2.0 — and CNSA 2.0 mandates **ML-KEM-1024** specifically (per [DoD CSA CNSA 2.0 Algorithms PDF](../research/pqc-compliance-scanner/sources.md), May 2025). A scanner that says "pqc_ready, satisfies CNSA 2.0" because it found `X25519MLKEM768` is materially wrong for the federal-NSS audience. | A US NSS operator scans a server that advertises only `X25519MLKEM768` and `mlkem768`; the scanner emits `verdict=pqc_ready, compliance_notes="meets CNSA 2.0 baseline"`; operator submits inventory; auditor flags it because CNSA 2.0 requires ML-KEM-1024. | Add `mlkem1024` (0x0202) and `SecP384r1MLKEM1024` to the v1 `TLS_PQC_NAMED_GROUPS` table (table cap stays ≤8, we still have headroom). Refine the M3 `compliance_notes` to say "**CNSA 2.0 baseline** (requires ML-KEM-1024)" only when ML-KEM-1024 is in the advertised set; otherwise say "**transitional PQ** (ML-KEM-768 hybrid; CNSA 2.0 requires ML-KEM-1024 by 2027-01-01)." |
| F-CEO-2 | CEO | defer | M3 verdict + threat model row "compliance fine" | Threat model already calls out: "scanner reports pqc_ready because the server advertised an ML-KEM group, but the server's certificate chain still uses classical RSA/ECDSA signatures, which fails the end-to-end PQ criterion auditors actually want." This is the most material false-positive risk for the buyer. v1 explicitly defers cert-chain analysis. The defer is reasonable for a one-week wedge — but worth pricing the v1.1 follow-up now so the user can decide whether the cert-chain work goes into a fast-follow runbook or into a stretched M3. | A federal contractor uses the scanner to attest "all 800 endpoints are PQ-ready"; cert-chain check is omitted; auditor pulls the cert from one endpoint and finds RSA-2048 sig; attestation rejected; contract impact. | Either (a) hold scope: v1 ships SSH+TLS group enumeration only; v1.1 (separate runbook) adds cert-chain via cross-reference to existing `ssl_certificate_scan` results stored in same SQLite event table — `compliance_notes` then includes "cert chain: classical RSA/ECDSA — see ssl_certificate_scan event for this target" when both modules run; OR (b) stretch M3 to include cert-chain note (~+0.5 week). Recommended: (a) — keep v1 wedge tight; create issue for v1.1. |
| F-CEO-3 | CEO | hold-scope | M3 — no Web UI panel | Runbook decides "no Web UI panel — auto-discovery is sufficient". Confirmed correct: the buyer (security engineer doing 800-endpoint inventory) consumes JSON, not a Web UI panel. Holding. | N/A | No change. |
| F-ENG-1 | Eng-lead | ask | §6 Global Red Lines: "No retries inside the library — each probe is single-shot. Nettacker's framework-level `retries` option still applies to the engine's `run()` call but the library MUST be idempotent and side-effect-free." | This contradicts itself. `BaseEngine.run()` at [base.py:288-293](../../../nettacker/core/lib/base.py#L288-L293) does `for _i in range(options["retries"]): try: response = action(**sub_step); break; except Exception: response = []`. If a probe raises an exception (e.g. `socket.timeout` not caught inside the library), the framework retries up to `options["retries"]` times — default 3 in Nettacker. That's 3× the connection load against a probed target per group, multiplied by 8 groups = 24 connections per (host, port) per scan. Threat model `tm-pqc-compliance-scanner-abuse-3` is the exact failure mode. | A CI cron job runs `nettacker -m pqc_scan -i <prod-fleet>`; one server is slow on 3 of 8 PQC groups; framework retries each ×3; load balancer connection-rate alarm pages oncall at 02:00. | Two options: (a) catch every `socket.*` exception inside `PqcLibrary.tls_pqc_scan` and `ssh_pqc_scan` so the framework never sees an exception (always returns a dict with `errors=[…]`), making framework retries a no-op for *probe* failures — only true bugs would re-execute; OR (b) set `retries: 1` in the YAML manifest's per-step config (if Nettacker's YAML schema supports per-step overrides — verify in M1 pre-flight). Strongly prefer (a) — defense in depth. Update §6 Global Red Lines and add to M1 invariant list: "library catches every recoverable network exception; framework never sees `Exception` from a probe call." |
| F-ENG-2 | Eng-lead | ask | M1 contract: "we ship the YAML referencing `tls_pqc_scan` from M1 even though the method is a stub" | Footgun: if M2 slips, production scans emit `verdict=unknown` + `errors=["tls_probe_not_yet_implemented"]` for every TLS port on every target. That's misleading-by-design output sitting in production for an unbounded period. | A user pulls feature/pqc-compliance-scanner after M1 lands but before M2; runs `pqc_scan`; gets `unknown` for every TLS port; reports "TLS scan is broken" as a bug. | Either (a) don't ship the TLS step in the YAML until M2 (M2's allow-list now includes the YAML edit), OR (b) ship M1's YAML with the SSH step only and let M2's contract explicitly add the TLS step. Recommended: (b). Edit M1 contract to drop the stub from `pqc.py`, edit M1 YAML scope to ship SSH step only, edit M2 to include the YAML manifest in its allow-list. |
| F-ENG-3 | Eng-lead | ask | M2 step 4: "dummy public-key bytes for PQC groups must be of the correct length per the IETF draft" | Step 4 names the constraint but does not pin the actual byte lengths anywhere in the design. Without a deterministic spec, the M2 implementer must consult IETF drafts during execution — they'll get this wrong and we'll mis-classify ServerHello vs decode_error responses. | Implementer sets `key_share` to 1216 bytes for `X25519MLKEM768` (correct: 32 + 1184 = 1216), but uses 1568 bytes for `MLKEM768` instead of the correct 1184; server sends `decode_error` alert; we record `errors=["decode_error_for_MLKEM768"]` instead of detecting that MLKEM768 IS supported. | Add a table to [interfaces.md](../design/pqc-compliance-scanner-interfaces.md) under a new heading "PQC named-group key_share lengths (IETF-draft-pinned)" with one row per codepoint and the exact public-key byte length cited to the IETF draft section number. Block M2 start until this table exists. |
| F-ENG-4 | Eng-lead | ask | §3 architecture diagram footnote / M1 compatibility checklist: "Web UI auto-discovers `pqc_scan` as a discoverable module without code changes" | Stated as fact but not verified. If Web UI auto-discovery requires editing a JSON manifest or static asset, M1 close-out fails this checkbox silently. | M1 lands; user runs `docker-compose up`; Web UI module dropdown does NOT show `pqc_scan`; user files bug "M1 didn't actually finish." | Add a pre-M1 evidence row: "Verify Web UI module discovery — read [nettacker/web/](../../../nettacker/web/) for module list source. If Web UI maintains a separate manifest, add to M3 allow-list." Reduces ambiguity at near-zero cost. |
| F-ENG-5 | Eng-lead | auto-fix | M2 BDD scenario "Server sends oversized record" | Threat model row `tm-pqc-compliance-scanner-abuse-2` is the canonical citation but the BDD row doesn't reference it; abuse-acceptance discipline says it should. | Reviewer reading M2 doesn't find the abuse-row binding without flipping to threat-model.md. | Edit BDD row to read: `Server sends oversized record (abuse-2)`. APPLIED in this critique. |
| F-ENG-6 | Eng-lead | hold-scope | M3 e2e against `tls13.1d.pw` and `github.com:22` | Public test endpoints can churn; runbook acknowledges with `xfail_strict=False`. Reasonable trade-off. | If `tls13.1d.pw` retires, e2e suite emits skip messages instead of failing CI. | Hold. Already addressed. |
| F-SEC-1 | Security | ask | M1 SSH KEXINIT parse path → log line | **Bug class: CWE-117 (Improper Output Neutralization for Logs / Log injection).** `SSH_PQC_KEX_ALGORITHMS` matching is against server-supplied name strings parsed out of MSG_KEXINIT. We then log them. RFC 4250 §6 restricts SSH algorithm name strings to `^[a-zA-Z0-9._@+-]+$`, but a hostile or malformed server may send arbitrary bytes inside the name-list strings. Threat-model row `tm-pqc-compliance-scanner-abuse-5` covers banner-string concerns but not KEXINIT-string injection. **Class elimination via input sanitization at parse boundary** — variant analysis playbook recommends "regex-restricted parse boundary; reject non-conformant strings into errors". | A hostile target advertises a kex algorithm string `mlkem768x25519-sha256\n[CRITICAL] suspicious activity from <attacker_ip>`; scanner logs the string verbatim; downstream log analyzer sees a forged CRITICAL line attributed to attacker_ip. | Add to M1 invariant list: "every string extracted from a server-controlled SSH name-list is validated against `^[a-zA-Z0-9._@+/-]+$` (RFC 4250 §6) at parse boundary; non-conformant strings dropped with an entry in `errors=['malformed_algorithm_name_<hex>']`." Add a BDD row "Server advertises malformed algorithm name" in M1. **CWE-117** mapping; **OWASP Top 10 A09 Security Logging and Monitoring Failures (variant: log injection)**; **ASVS V7.3.1** (log-injection prevention). |
| F-SEC-2 | Security | ask | M2 `_parse_tls13_server_response()` | **Bug class: CWE-787 (Out-of-bounds Write) / CWE-770 (Allocation of Resources Without Limits).** The TLS response parser consumes adversary-controlled bytes. Threat-model row `tm-pqc-compliance-scanner-abuse-2` covers oversized-record byte-cap, but does not cover *malformed-but-within-cap* responses (e.g., a record claiming length=200 but only containing 50 bytes; or a handshake header claiming length=10000 inside a 16384-byte record body). Variant-analysis playbook recommends fuzzing parse boundaries. | A honeypot in scope returns a TLS record with header `type=22, length=0xFFFF` then 5 bytes; the parser reads only 5, attempts to interpret as ServerHello, computes `extensions_length` from out-of-buffer bytes, raises an unrelated exception that escapes the library, framework retries 3×. Or worse: the parser fails to validate the inner handshake length and accesses memory past the buffer (Python is bounds-checked so this is a `IndexError` not memory corruption — but it still escapes the library). | Add a parameterized "torture" test in M2 BDD: 100 randomly-mutated ServerHello byte strings (seeded RNG for reproducibility); assertion is "no exception escapes `_parse_tls13_server_response`; every input either returns a tagged result or returns an `errors` entry." Add to M2 invariants: "`_parse_tls13_server_response()` is total — every `bytes` input maps to a tagged result; no exception ever escapes." **CWE-787 / CWE-770**; **ASVS V5.1.3** (input-size validation), **V5.1.4** (input-format validation). |
| F-SEC-3 | Security | ask | M1 + M2 socket close discipline | **Bug class: CWE-404 (Improper Resource Shutdown / FD leak).** Threat model says "every code path closes socket" but no test asserts this. A library used in CI fan-out that leaks FDs eventually exhausts the process's `RLIMIT_NOFILE`. | A user runs `nettacker -m pqc_scan` against 5,000 targets in CI; one specific server consistently triggers a code path that doesn't close the socket; after 1024 targets, all subsequent probes fail with `EMFILE`; user blames Nettacker. | Add a BDD row in both M1 and M2: "FD count delta is zero after probe completes (success or failure path)." Implementation hint: use `psutil.Process().num_fds()` (Linux/macOS) snapshot before/after; or `os.listdir('/proc/self/fd')` length check. Alternative: use a `contextlib.contextmanager` for socket lifecycle and assert no socket survives — but the test is what catches regressions. **CWE-404**; **OWASP ASVS V11.1.1** (resource exhaustion). |
| F-SEC-4 | Security | hold-scope | DNS rebinding / SSRF via `--target` flag | Bug class CWE-918 (SSRF). Operator-supplied target argument can resolve to internal IP at scan time. **N/A — Nettacker's existing concern, not introduced by this module.** Inherited from existing scan modules. | (existing concern) | Hold. |
| F-SEC-5 | Security | hold-scope | Banner spoofing | Bug class CWE-200. Already addressed in threat model `tm-pqc-compliance-scanner-abuse-5`: transparent `SSH-2.0-Nettacker` banner is the documented choice (Nettacker is a pen-test tool; honeypot logging is acceptable transparency, not an information leak). | (handled) | Hold. |
| F-DES-0 | Design | N/A | M3 — no UI surface added | This runbook adds zero new UI components. Web UI auto-discovers the module from YAML. CLI / JSON output is the only user-facing surface. Design persona pass is N/A per skill rules. | N/A | Skipped. The single design-overlap concern (`compliance_notes` string content rendered in HTML by Web UI) — the strings are operator-trusted constants from the algorithm tables, not server-controlled, so HTML-injection is N/A. |

## Auto-fixes applied

- **F-ENG-5**: Added `(abuse-2)` annotation to M2 BDD row "Server sends oversized record". Applied below.

## Asks awaiting user decision (8 items)

1. **F-CEO-1** — add `mlkem1024` to v1 table + refine `compliance_notes` for CNSA 2.0 honesty.
2. **F-CEO-2** — confirm v1.1 cert-chain follow-up is a separate runbook (recommended) vs. stretched M3.
3. **F-ENG-1** — catch all `socket.*` exceptions inside library so framework retries become a no-op for probe failures (defense in depth against threat-model abuse-3).
4. **F-ENG-2** — drop the stub `tls_pqc_scan` from M1; have M1 ship SSH step in YAML only; M2 adds the TLS step to YAML.
5. **F-ENG-3** — add IETF-pinned key_share byte-length table to interfaces.md before M2 starts.
6. **F-ENG-4** — verify Web UI auto-discovery is real (M1 pre-flight evidence row).
7. **F-SEC-1** — sanitize SSH KEXINIT name-list strings against RFC 4250 §6 charset before logging (CWE-117 log injection).
8. **F-SEC-2** — add fuzz/torture test for `_parse_tls13_server_response` (CWE-787 / CWE-770 parse bug class).
9. **F-SEC-3** — add FD-leak BDD row in M1 and M2 (CWE-404).

## Recommendation to user

Accept asks 1, 3, 4, 5, 6, 7, 8, 9 (8 of 9). They are concrete, additive, and tighten the wedge without expanding scope. Defer ask 2 (cert-chain follow-up) to a fast-follow v1.1 runbook so v1 stays one week.

Holds (no action): F-CEO-3 (no Web UI panel), F-ENG-6 (e2e endpoint churn tolerated), F-SEC-4 (SSRF inherited), F-SEC-5 (banner transparency).
